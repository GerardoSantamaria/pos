spring.application.name=POS-GSZ
# Perfil activo
spring.profiles.active=dev

# Configuración común para todos los perfiles
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.show_sql=false

# Configuración de seguridad (Spring Security)
spring.security.user.name=admin
spring.security.user.password=$2a$10$dPwvYjDyRG8BEPfbsV0Qhee4hQDpCNQC7aDRr9HaPjbEtZtFvE1Ra

# Configuración de perfil dev (H2)
# Estas propiedades se activan solo cuando el perfil es 'dev'
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
spring.datasource.url=jdbc:h2:file:./data/pos-db;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.username=sa
spring.datasource.password=password
spring.datasource.driver-class-name=org.h2.Driver

# Configuración de perfil prod (PostgreSQL)
# Estas propiedades se activan solo cuando el perfil es 'prod'
# spring.datasource.url=jdbc:postgresql://localhost:5432/pos_db
# spring.datasource.username=pos_user
# spring.datasource.password=${POS_DB_PASSWORD}
# spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración de la aplicación
app.barcode-reader.enabled=true
app.barcode-reader.timeout-ms=250

# Configuración de seguridad adicional
app.security.jwt.secret=${JWT_SECRET:5D37A4E8F1D22B5B079D3A1A7F27DF39C26C6D6A}
app.security.jwt.expiration-ms=86400000
